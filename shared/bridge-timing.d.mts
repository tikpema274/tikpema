// Types for shared/bridge-timing.mjs — the client imports it directly, so tsc needs a declaration.
// ⛔ The VALUES live in the .mjs and only there; this file declares shapes, never numbers.
export const MINT_DEADLINE_MINUTES: number;
export const MINT_TIMING: string;
export const BURN_TIMING: string;
export const BRIDGE_TIMING: string;
