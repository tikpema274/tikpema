node_modules
.env
dist
.netlify
npm-install.log
scratchpad-netlifydev.log

# ── COLD KEYS. Never committed, never in .env, never read at runtime. ──────────
# The cold owner EOA for the ERC-8004 agent identities lives here. It is the root
# of trust for the agent registry: whoever holds it OWNS the agents. It is
# deliberately NOT in .env, because .env is loaded by the app and mirrored into
# Netlify env — a key that no running code path can reach cannot be leaked by one.
# Broad patterns, so a future key file cannot miss the net by being named slightly
# differently.
.keys/
*.key
*.privkey
cold-owner*.json

# ── Incidental run output from scripts/register-identity.mjs ───────────────────
# Raw dump of this wallet's NFT holdings, written as audit evidence for the
# existence check. Not credentials — just noise that shouldn't be committed
# carelessly.
# NOTE the deliberate asymmetry: agent-metadata/REGISTERED-IDENTITY.json is NOT
# ignored. The agentId is public, persisting it is the entire point of that
# script, and committing it is a durable copy of the value whose loss caused the
# orphan-wallet excavation in the first place.
agent-metadata/.nft-enumeration.json
